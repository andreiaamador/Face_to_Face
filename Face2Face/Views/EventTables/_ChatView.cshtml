@model ICollection<Face2Face.Models.MessageTable>
    
    <ul id="discussion">
	 
	    @foreach (var msg in Model)
        {
            @msg.UserProfile.Name
            @msg.Message
        } 
	
    </ul>

// fazer uma lista para fazr display das mensagens que já existem na base de dados... Esta vai ser a partial view que vai ser atualizada
No controlador, tenho que passar uma lista de mensagens à view para conseguir mostrá-las. Ou seja, tenho que passar um modelo.